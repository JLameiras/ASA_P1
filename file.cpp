#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int main() {
    ifstream inputFile ("input");
    if(inputFile.is_open()){
        while(getline(inputFile, line)){

        }
    }

    return 0;
}
